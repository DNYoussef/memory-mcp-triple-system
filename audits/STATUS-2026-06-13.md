# Memory MCP Triple-System - Live Audit Status

**Date:** 2026-06-13
**Basis:** Full suite. Committed: module-blocker baseline 75de1e5, G7 (0d8cfe7, 0a79c44),
G3/G11 (857aa64, 09abcef, 07282a7, 04362a3), retrieval cluster (358ea67, 8d76d00, 196c815,
82b5c3f), final 5 (be1f0f7, 8c42a62, 17a9453, 8836e8d), G8 (a6b7b79, 879e89a), A3 (7e4ede8,
3b673e4), G9/G10 (8d74c41, 673e1bd), G6 (e0b0587, 5ceb11e), G4/G5 (e1475a9, 3c8142a), D5 (8e46402,
b556f60), D8 (67063f6, 5b613bd), D2 (84af1b1, e23af60), D7 (0986364 fix, 60e7882 board),
V-QWEN (cd07182, board 53cf9b3), E4 (2025407 test, a2aa632 board, recon e5ac5f6),
E5 (358494a fix, 22d4aaf board), E6 (e8fcf9b fix, 3dcc7f2 board), E7 (135f513 fix, 2443b56 board),
E8 (48d72e3 fix, faab673 board), E9 (df94281 fix, bfd330b board), E10 (5137077 fix, d3cfc01 board),
E11 (a5d208f fix, 6df80dc board), H3 (f034eed fix, 5dbb870 board), H4 (25592f5 fix, 22f003d board),
H5 (748e55d fix, a04b605 board), H6 (8b5c21b fix, d0dd03b board),
H7 (1e5263f fix, 798c373 board), H8 (c30f63d fix, 68001e0 board),
H9 (03fedf4 fix, cb6cd68 board),
H10 (3fabb6c fix, 3cdb194 board),
H11 (2c60c26 fix, 824d168 board),
G12 (264dfda fix startup scripts + test_startup_scripts.py; this board commit next).
**Result:** 1342 passed, 0 failed, 6 skipped (coverage gate excluded - see Section 4).
  E4 (rekindle last_accessed_ts) verified-fixed-and-pinned (+1 test, no source change). E5 (fake dedup
  counts) is a REAL fix: run_deduplication no longer reports candidates as merges/freed bytes, and
  (review-caught, two rounds) no longer over-counts bytes_reclaimable via a directional pair filter
  (swapped-orientation pairs) nor via per-pair summing across duplicate clusters (+5 tests). E6 (hour-0-only
  cleanup) is a REAL fix: archival/cleanup sweeps now gate on elapsed time since last run, not wall-clock
  hour, so a sweep is never skipped because the process was not alive at hour 0 / a multiple of 6 (+2 tests).
  E7 (no cross-scheduler lock) is a REAL fix: the singleton MemoryLifecycleManager now carries one
  threading.RLock acquired by every mutating entry point (demote/archive/cleanup/rekindle/make_rehydratable/
  consolidate) via a @guarded_mutation decorator, so concurrent to_thread workers cannot interleave Chroma
  mutations (+2 tests). MemorySweeper is not wired into the live server, so it is outside the current race.
  E8 (str(dict) archive parse) is a REAL fix: rekindle no longer fabricates "file_path: /default/path.md"
  when metadata is missing nor returns /default/path.md on a parse miss; it parses the production JSON
  metadata (structured-parse-first), keeps tolerant legacy-string scanning, and fails honestly (None +
  warning) so a parse miss can no longer silently rehydrate the wrong file (+2 tests).
  E9 (lossy summary bound) is a REAL fix: _summarize replaced the miscalibrated min(200, max(50, len/5-1))
  bound (which truncated within-budget short docs to ~50 chars + "..." for a trivial storage saving) with a
  single documented SUMMARY_MAX_LEN=200 budget - docs within budget are kept verbatim (zero loss), only
  larger docs are compressed to <= the budget (+2 tests, test_archive_compression recalibrated).
  E10 (recency tiebreaker) is a REAL fix: the hash-duplicate _sort_by_priority folded recency in as
  (1 if c.created_at else 0) - a constant - so "oldest wins" was never implemented and equal tier+confidence
  ties fell back to input order. Replaced with a lexicographic tuple key (tier, confidence, oldest-first),
  now consistent with _determine_primary (+2 tests; E5 orientation/cluster test docstrings refreshed since the
  hash phase now agrees with the embedding phase on oldest=primary).
  E11 (DriftSeverity.NONE ValueError) is a REAL fix: get_candidates_for_cleanup built a severity_order list
  that omitted DriftSeverity.NONE, then called severity_order.index(signal.severity); since _score_to_severity
  returns NONE below the LOW threshold (e.g. detect_confidence_drift just under the confidence threshold emits
  a NONE-severity signal into report.signals), .index(NONE) raised ValueError. NONE added as the lowest rung
  (+3 tests). WHOLE lifecycle E-cluster (E4-E11) now done + tested.
  H3 (weighted_min discards weighting) is a REAL fix: the quality-gate weighted_min branch computed
  weighted_scores = [s.score * w] but then returned scores[min_idx].score (the RAW score of the min-weighted
  check), so gate_weights never affected overall_score. Now returns the weighted minimum itself, clamped to
  [0,1] (a weight > 1 can exceed 1.0). Starts the validation H-cluster (+3 tests).
  H4 (rule remove unguarded) is a REAL fix: _apply_change's remove branch did content.replace(current_value,
  "") with no guards - a silent no-op + false success when the value was absent, and a file-wide strip of
  every occurrence. Now guards non-empty + present (in both dry-run and deployment, matching modify) and
  removes a single occurrence (count=1); _verify_change checks a recorded post-remove count so it neither
  rejects a legitimately-remaining second copy nor passes an unapplied remove (+7 tests).
  H5 (category-drift fabricated from a 0.5 default) is a REAL fix: _detect_category_drift compared first- vs
  second-half success rates over the UNION of categories, defaulting a category missing from one half to 0.5
  and so fabricating drift >= 0.15 (false rule proposals) from data that did not exist. Now compares only the
  INTERSECTION (categories present in both halves) - a missing baseline yields no drift (+2 tests).
  H6 (daily bucket labeled one day early) is a REAL fix: get_daily_breakdown builds rolling 24h windows
  ending at now (day_end = now - i days; day_start = day_end - 1 day) but labeled each bucket with day_start,
  so the most recent bucket (ending now) read as YESTERDAY. Label by day_end instead; windows and counts
  unchanged, timezone unchanged (+2 tests).
  H7 (quality trend statistic mismatch) is a REAL fix: _analyze_metric used first-half/second-half means for
  previous_value, change_percent, and direction, but reported current_value and warning/critical thresholds from
  values[-1]. A good final sample could hide a bad current-period average (and vice versa). current_value now
  uses the same second-half mean as the trend math, so threshold flags and reported values are based on one
  statistic (+2 tests).
  H8 (namespace key truncation/collision) is a REAL fix: build_namespace_key broke on the first missing
  segment, dropping later populated segments and colliding sparse keys. It now emits empty placeholders only for
  interior missing segments before the last provided value, preserving positional meaning for later fields without
  padding trailing omissions; parse_namespace_key ignores empty placeholders so later segment names still
  round-trip correctly (+3 tests).
  H9 (approval rate > 1.0) is a REAL fix: auto-approved proposals incremented approved but did not increment
  total_submitted, so the approval-rate numerator could exceed the completed-submission denominator. total_submitted
  now increments once for every submitted proposal before the auto-approval branch, keeping auto and manual
  accounting on the same base (+2 tests).
  H10 (outcome recall cap) is a REAL fix: get_outcomes_by_category and get_outcomes_by_type pre-truncated
  their index scans to the newest limit*2 ids before applying missing-record and since filters, so valid older
  in-window outcomes could be missed behind a tail of stale records. Both paths now share a helper that scans the
  full index newest-first, applies filters, then stops at limit (+2 tests).
  H11 (tag-scorer cache key / shared cached dict) is a REAL fix: assign_tags cached by content[:300].lower()
  only and returned the cached mutable dict directly. Two contents sharing the first 300 chars collided even when
  later text changed the project tag; two contexts for the same content collided even when agent_id changed; and
  caller mutation of returned TagAssignment objects corrupted the cached result. Cache keys now hash the full
  content plus context, and cached assignments are deep-copied on both store and read (+3 tests).
  G12 (startup/tunnel automation) is a REAL ops fix: the PowerShell server launcher now uses command-line process
  detection via Win32_Process, treats an already-healthy server as success, refuses to start a duplicate when a
  matching process is unhealthy, launches the repo venv python directly from the repo root, and fails if the server
  never becomes healthy. The tunnel launcher now refuses to expose a public URL unless the local /health endpoint is
  healthy, detects existing cloudflared tunnels by command line, verifies saved tunnel URLs before reporting them,
  removes stale URL files, uses the resolved cloudflared path, and uses a unique temp output file. The tracked batch
  launcher now runs from the repo root instead of scripts/ so its venv path resolves (+4 static smoke tests).
  V-QWEN qwen test hermetic (cd07182); suite deterministically green on this machine.
  Trajectory: ... -> H7 1330/0 (quality trends use one current statistic) -> H8 1331/0 (namespace placeholders preserve later fields) -> H9 1333/0 (auto-approvals counted in submitted base) -> H10 1335/0 (filter before outcome limit) -> H11 1338/0 (tag-scorer cache key + copied cached values) -> G12 1342/0 (startup/tunnel automation).
  Suite GREEN; zero regressions across the whole engagement.
**Purpose:** Replace the static MECE inventory (MECE-merged-audit-2026-06-12.md) as the
current source of truth. The MECE doc is the issue catalog; this is the live board.

Failure classes used below:
- **PRODUCT** - real defect in shipped code; maps to a MECE ID (or a NEW id).
- **REGRESSION/INCOMPLETE** - a MECE item marked fixed whose symptom test still fails.
- **TEST-STALE** - code behavior is plausibly correct; the test encodes an old contract.
  Needs triage (update test vs fix code) before it counts as a product bug.

---

## 1. Fixed and verified (this engagement)

### Module-blocker pass (2026-06-13, commits 18026e5..75de1e5)
| ID | Fix | Verification |
|----|-----|--------------|
| F6 | Declared flask + aiofiles in both manifests | Collection green (1264, was 2 errors); 13 formerly-blocked tests pass |
| F7 | Curation service built lazily, not at import | 20/20 test_curation_app pass; import leaves curation_service None, no ./data/chroma |
| I3 | stdio_server `__all__` lists defined getter names | ruff F822 clean repo-wide; all `__all__` names resolve |
| F3/F4 | 7 WoundHealer/RLM files resolve paths from env | Compile + runtime: defaults host-equivalent, env overrides honored, no hardcoded host paths remain in src/woundhealer or src/rlm |

Net regressions introduced by this pass: **0** (A/B-diffed vs clean HEAD; the only
deltas were 13 curation tests broken then fixed within the pass).

### Reliability pass (2026-06-13) - from test evidence
| ID | Fix | State / verification |
|----|-----|--------------|
| G7 | Injector rule isolation (deepcopy of DEFAULT_RULES) + trigger-confidence blend; activity-detector cooldown recorded in `finally` on injection failure | commits 0d8cfe7, 0a79c44; 7 tests + regression test |
| G3 | Vault-sync normalizes ndarray-or-list embeddings (the incomplete-fix vs ingestion); stale chunk_file mock -> chunk_text | commits 857aa64 + 07282a7; 2 Obsidian sync tests pass |
| G11 | BeadTask Optional fields default None (over-strict ctor); single env-first beads-binary resolver (MEMORY_MCP_BEADS_CLI/BEADS_BINARY) across 4 surfaces; async `_run_command` + sync-wrapper `_run_coroutine_sync` timeouts | commits 09abcef + 07282a7; 3 dataclass + 2 timeout regression tests |
| B1a | HippoRAG keyword fallback when NER extracts nothing (confirmed B1 fully fixed; this was entity extraction, not graph-to-chunk mapping) | commit 358ea67; 22 hipporag tests + helper test (integration) |
| D-nexus | Nexus filter on strongest per-tier evidence instead of the deflated weighted-sum hybrid (vector-only matches were systematically dropped) | commit 8d76d00; 22 e2e + 124 nexus tests + 2 helper unit tests |
| vector-search | TEST-STALE: tests aligned to the code's deliberate ValueError contract (asserts stripped under -O) | commit 196c815; 15 tests |
| final-5 | observation-timeline tz basis (now->utcnow PRODUCT), tagging behavioral test (TEST-STALE), ontology nested-metadata read + graph-based beads query (2 PRODUCT), tool-count name-set assertion (TEST-STALE) | commits be1f0f7/8c42a62/17a9453; per-file suites green |

Net regressions across reliability + retrieval + final-5: **0** (24 -> 17 -> 12 -> 5 -> 0 failing, each step verified).

### Backlog pass (2026-06-13) - untested items from Section 3
| ID | Fix | State / verification |
|----|-----|--------------|
| G8 | SessionStart hook crashed on non-ASCII memory content (print() under cp1252 stdout -> UnicodeEncodeError escaping main(), no context injected). Added _emit_context() writing raw UTF-8 bytes via stdout.buffer (never raises); moved KVStore() inside main()'s try, guarded close(), and a graceful-degradation except (review caught the KVStore/close gaps). | commit a6b7b79; 4 regression tests (utf8 emit / no-raise on cp1252 / ascii roundtrip / main no-crash on KVStore failure). |
| A3 | ConfigGuard fail-OPEN: VERIFIED already fixed in the prior pass (5adb4df - portable NamedTemporaryFile + fail-CLOSED BLOCK, replacing the hardcoded /tmp write that returned ALLOW on the Windows failure). Was untested. No new code change - confirmed via git history that the pre-5adb4df gate_change used /tmp and returned passed=True/ALLOW on except. | commit 7e4ede8; 3 regression tests (fail-closed on scan raise / secret BLOCKed end-to-end on Windows / clean config not blocked). Tests fail against pre-5adb4df code, pass on fixed. |
| G9/G10 | request_router handler robustness. G10: _format_result_full direct-indexed text/score/file_path -> KeyError on the vector-fallback shape (most-used tool returned isError); now .get() defaults like the compact formatter. G9: handle_bayesian_inference had no try/except (any extraction/inference error -> opaque isError) and returned "null" on a None result; now guards, reports the actual error, handles None and empty-entity clearly. (network=None kept - query_conditional falls back to the engine's stored network, ISS-018.) | commit 8d74c41; 5 regression tests (4 fail against pre-fix code, all pass after). |
| G6 | TimeScheduler._check_schedules had no try/except in the while loop, so one bad schedule (matches_now raising, or _schedules mutated mid-iteration) escaped the loop -> the task died and _running stayed True (no restart), stopping ALL scheduling. Guard is PER schedule (inside the for loop, re-raising CancelledError so stop() still works) so one bad schedule is skipped and the rest still run; iterate a snapshot. (Review caught a first attempt that wrapped the whole cycle - that kept the task alive but a bad schedule still blocked every later one.) | commit e0b0587; 2 regression tests (survives a failing schedule + bad-then-good: a later good schedule is still evaluated). Both fail against pre-fix code. |
| G4/G5 | file_watcher.py. G4: _on_file_event ran asyncio.get_event_loop() in the watchdog thread (raises on Py3.10+; the bare except silently dropped EVERY file event). Capture the loop at start() and use self._loop.call_soon_threadsafe. G5: add_watch_path created a NEW FileEventHandler per path (duplicate handler -> double-fire); now reuses one shared self._handler, and the existing same-path dedup prevents re-scheduling. | commit e1475a9; 2 regression tests (event from a non-loop thread reaches the queue; add_watch_path reuses the shared handler and does not re-schedule a known path). Both fail against pre-fix code. |
| D5 | pgmpy network=None fallback for marginal/MPE. VERIFIED already fixed: query_marginal delegates to query_conditional (fallback since b5419b3f); get_most_probable_explanation got its effective_network fallback + None guard in fe2f9946 (priority pass). Pre-fe2f9946 MPE passed network directly, so network=None crashed in _map_query_impl. No new code change. | commit 8e46402; 4 regression tests (marginal & MPE: network=None uses stored network / returns None with no stored network). The two MPE tests crash against pre-fe2f9946 code (git-confirmed). |
| D8 | _get_edge_posterior CPD cell read. VERIFIED already fixed in fe2f9946: was `float(cpd.values.flatten()[0])` - .flatten() crashes on lightweight List[List[float]] CPDs, and [0] read the state-0 cell, not the positive state. Now _cpd_values_as_lists (ndarray-or-list, no flatten) + _positive_state_row (correct positive cell). git -S confirms .flatten() was removed in fe2f9946. No new code change. | commit 67063f6; 4 regression tests (positive-state row; ndarray-or-list values; end-to-end lightweight no-flatten + pgmpy reads positive cell not state-0). End-to-end tests fail against pre-fe2f9946 (crash / 0.8 vs 0.2). |
| D2 | reranker blend normalization. VERIFIED already fixed in fe2f9946: merge_scores now clamps hybrid to [0,1] (line 207) AND sigmoid-normalizes rerank (line 209) before the weighted blend - pre-fix line 210 blended the RAW hybrid (`0.5*hybrid + 0.5*rerank_norm`), so un-normalized hybrid (>1) skewed the "50/50". git -S confirms the hybrid clamp was added in fe2f9946. Existing tests only used hybrid<=1, so the clamp was untested. No new code change. | commit 84af1b1; 2 regression tests (un-normalized hybrid 2.0 clamped -> 0.75 not 1.25; both inputs in [0,1] so final<=1). The clamp test fails against pre-fe2f9946 (1.25). |
| D7 | Bayesian VAR=state pseudo-doc leakage - the ONE genuinely-open D item (a real fix, not a pin). _query_bayesian_tier emits {var}={state} rows (id "bayesian_<var>_<state>") into recall; they flowed through fusion and appeared in core/extended as if they were retrieved documents. Fix: process() drops them right AFTER rank and BEFORE rerank/compress, via _is_bayesian_pseudo_doc - a STRUCTURAL predicate (source_tiers==["bayesian"] AND metadata variable/state AND text exactly "VAR=state"), NOT an id prefix. Two review findings drove the design: (a) ids are now caller-stable data, so filtering id.startswith("bayesian_") dropped a real vector doc with id "bayesian_real_doc"; (b) filtering in _step_compress ran after _step_rerank, so pseudo-rows consumed rerank slots and skewed rerank stats. Both fixed. Rows still contribute signal through recall/fusion/feedback but never reach the reranker or results. | commit 0986364 (board 60e7882); 3 tests (output-leak: present in recall, absent from process() results, real doc still returned; over-filter negative: real "bayesian_real_doc" vector doc survives; placement negative: reranker receives ["doc1"] only, not the pseudo-row). All 3 fail against the id-prefix-in-compress version (over-filter assert False; reranker sees both ids). |
| E4 | Rekindle dropped last_accessed_ts. VERIFIED already fixed in 5adb4df (same priority pass as A3): pre-fix _reindex_and_promote built a FRESH metadata dict with only last_accessed (ISO) + rekindled_at and NO last_accessed_ts, and discarded the archived metadata entirely; the re-indexed chunk had no metadata at all. A rekindled chunk therefore had no last_accessed_ts, never matched the demote query {"last_accessed_ts": {"$lt": cutoff}}, and looked permanently active/unclassifiable. The fix merges archived metadata and sets last_accessed_ts = now.timestamp() (intentional refresh). No new code change. | commit 2025407 (board a2aa632); 1 test (archived chunk carries a 120-day-old last_accessed_ts; after rekindle the promoted vector metadata AND the re-indexed chunk metadata both carry a valid float last_accessed_ts bounded by [before, after] and newer than the stale ts; downstream demote cutoff would have demoted the old ts but not the fresh one). Fails against pre-5adb4df: "promoted: last_accessed_ts dropped to None/missing". |
| E5 | Fake dedup counts - GENUINELY OPEN (a real fix, not a pin). TierDeduplicator.run_deduplication is analysis-only (it holds no store; it FINDS pairs and plans via get_merge_plan, never updates/deletes), yet when not dry_run it reported duplicates_merged = len(all_pairs) (candidates relabeled as merges) and bytes_freed = sum of candidate byte_sizes (potential relabeled as freed). No caller wires this engine, so nothing actually merged. Fix part 1: report actual work honestly (duplicates_merged=0, bytes_freed=0) and move the potential to a new, additive bytes_reclaimable field; dry_run kept for API compat but no longer masks the fabrication. Fix part 2 (review-caught): the cross-phase pair filter was DIRECTIONAL ((primary,duplicate) tuple), so the same two chunks paired with primary/duplicate swapped across phases (exact-hash vs semantic) survived - inflating duplicates_found AND double-counting bytes_reclaimable (reviewer repro: found 2, reclaimable 333). Now filtered by orientation-independent _pair_key (frozenset of the two ids), so each unordered pair counts once. Fix part 3 (review-caught, round 2): even with unique pairs, a duplicate CLUSTER of 3+ identical chunks yields multiple distinct pairs sharing one duplicate target (newest->middle and oldest->middle both name `middle`), and per-pair summing counted that target's bytes twice (reviewer repro: 3 chunks -> found 3, reclaimable 555; unique duplicate bytes 333). bytes_reclaimable now sums each UNIQUE duplicate chunk_id once (seen_duplicate_ids guard) - a chunk reclaims its bytes once when removed regardless of how many pairs nominate it. Consolidation.py accounting was checked and is already honest (count increments only after a real update+delete). | 358494a; 5 tests (no duplicates -> all zero; one real same-hash duplicate -> found==1 but merged==0/freed==0 and reclaimable==byte_size, incl. to_dict; dry_run vs not give identical honest accounting; same pair via hash+embedding in opposite orientation -> found==1 and reclaimable not double-counted; 3-chunk cluster -> reclaimable == unique-duplicate-bytes < naive per-pair sum). Fail against the broken versions: "candidates must not be reported as merged: assert 1 == 0" (fabrication), "the same two chunks were paired twice: assert 2 == 1" (directional filter), and "assert 555 == 333" (per-pair cluster over-count). |
| E6 | Hour-0-only cleanup - a real fix. _run_loop gated archival on `current_hour % 6 == 0` and expired-entry cleanup on `current_hour == 0` (wall-clock midnight). The loop sleeps ~3600s, so if the process was not alive at that exact hour - or the loop drifted past the boundary - the sweep was skipped entirely and stage stats carried unswept expired rows. Root cause (R1/R2): gating on an absolute wall-clock hour instead of elapsed time. Fix: track _last_archival/_last_cleanup timestamps and gate on ARCHIVAL_INTERVAL (6h) / CLEANUP_INTERVAL (24h) elapsed since last run; None means "never yet" so the first iteration runs it (sweeping anything that piled up while the process was down). Same cadence (4x/day archival, 1x/day cleanup) but anchored to elapsed time, not the clock, so a missed hour-0 no longer skips a day. demotion still runs every cycle. | e8fcf9b; 2 tests (start at 03:00 -> archival AND cleanup still run within the first cycle, not skipped; two cycles 1h apart -> demotion runs both, archival/cleanup run once each since 1h < 6h/24h thresholds). Both fail against the wall-clock version: "Expected 'archive_demoted_chunks' to have been called once. Called 0 times" at hour 3. |
| E7 | No cross-scheduler lock - a real fix. The lifecycle mutators all run on the SINGLETON MemoryLifecycleManager and the live server invokes them via asyncio.to_thread (real worker threads, genuinely concurrent): LifecycleScheduler._run_loop calls demote/archive/cleanup; the /tools/consolidate endpoint calls consolidate_similar; ingestion-service store() calls demote+archive. They mutate the SAME Chroma collection with no shared lock -> delete-after-read / double-delete / phantom archive races. (MemorySweeper is a third subsystem in the audit but is NOT wired into http_server - no register_callback, start() only in its own convenience fn - so it is outside the live race; if it is ever wired it must route through the same manager/lock.) Root cause (R1): no serialization data structure. Fix: add one threading.RLock (_mutation_lock) to the manager and a leaf-module @guarded_mutation decorator (src/memory/_mutation_lock.py, imports nothing from the package -> no cycle) applied to every mutating entry point across the mixins - demote_stale_chunks, archive_demoted_chunks, make_rehydratable (stage_transitions), cleanup_expired, rekindle_archived (lifecycle_manager), consolidate_similar (consolidation). RLock (not Lock) so a guarded method calling another on the same thread re-enters instead of self-deadlocking; not asyncio.Lock because the contended sections run in to_thread worker threads, not the event loop. All mutators guarded uniformly (R2) so no unguarded mutator can reintroduce the race. | 135f513; 2 tests (black-box concurrency probe instruments collection.get/kv.cleanup_expired, a Barrier releases two worker threads into their critical sections together, asserts max_concurrent==1: distinct mutators archive vs cleanup serialize - proving the lock spans methods, not just per-method re-entrancy; two concurrent archive sweeps serialize). Both fail against the unlocked version: "assert 2 == 1" (both threads inside the critical section at once). 44 existing lifecycle/gate4/ingestion unit tests still green. |
| E8 | str(dict) archive parse - a real fix. rekindle_archived resolves a chunk's file_path from archived metadata. Two footguns made a parse miss silently rehydrate the WRONG file: (a) _get_archived_data injected a fabricated "file_path: /default/path.md" string when no metadata key existed ("# Fallback for tests"); (b) _extract_file_path string-split the raw metadata and returned "/default/path.md" on the except. Net: a missing/garbled metadata entry caused rekindle to read /default/path.md instead of failing. Reality check: BOTH production writers store JSON - _archive_chunks_batch does json.dumps(metadata, sort_keys, default=str), and make_rehydratable re-writes that JSON verbatim; the "file_path: value" colon format only ever existed in tests. _metadata_from_string already parses JSON (+ ast.literal_eval for legacy python-repr). Fix: drop the /default/path.md fabrication in _get_archived_data (return real None when metadata absent); rewrite _extract_file_path structured-parse-first - if the parsed dict has file_path return it; if it parsed to a non-empty dict without file_path return None (do NOT scan, a "file_path" substring elsewhere would mislead); only when the parse yields nothing fall back to a tolerant raw-string scan for legacy non-JSON metadata, and on any miss return None + warning, never a default path. Backward-compat preserved (R3): JSON, legacy python-repr, and legacy colon strings all still resolve; only the silent wrong-path fabrication is gone. Parallel _load_archived_metadata "archived_at" split in stage_transitions is a different field/method - left as-is (out of E8 scope). | 48d72e3; 2 tests (missing metadata -> rekindle False AND _read_full_text is never called with /default/path.md and index_chunks not called - fails old code: "rekindle silently read the fabricated /default/path.md", read_paths==['/default/path.md']; production JSON metadata -> rekindle True, reads the real temp file, re-indexed chunk carries the real file_path + full text). 26 lifecycle_manager tests green incl. the pre-existing legacy colon-format rekindle tests. |
| E9 | Lossy summary bound miscalibrated - a real fix. Archival keeps ONLY the summary (the chunk is deleted from the vector store), so _summarize's length bound is the bound on irreversible loss. The old bound max_len = min(200, max(50, int(len/5)-1)) was miscalibrated at both ends: for docs in (50, 255] it floored at 50 and truncated a complete short document to ~50 chars + "..." (needless loss for a trivial cold-storage saving), while for large docs the flat 200 cap gives 300:1-1500:1 (the log/docstring claim "100:1" but the formula never targets it). Empirically a 115-char 2-sentence doc summarized to "Alpha Bravo met Charlie at Delta Station near..." (45 chars). Fix (R1/R2/R5): one documented module constant SUMMARY_MAX_LEN=200 as the budget; if len(full_text) <= budget keep it VERBATIM (zero loss when the saving is trivial), else run the existing extractive single-sentence selection and truncate to the budget. Strictly reduces loss vs the old bound, never increases it; large-doc behavior (<=200) is unchanged. The single-sentence extraction design itself is unchanged (out of scope - E9 is the bound calibration). Review-caught: also removed the now-false "compressed 100:1" claim from the module docstring (lifecycle_manager.py) and the archive log line (stage_transitions.py) -> "compressed to bounded summary" (no behavioral change; no test asserts the string). | df94281; 2 tests (a within-budget multi-sentence doc, 50 < len <= 200, returns verbatim with no "..." - fails old code which truncated it to ~45 chars; an oversize doc returns a summary <= SUMMARY_MAX_LEN and < source and > 0). test_archive_compression recalibrated from "< len/5" to "<= SUMMARY_MAX_LEN and < source" (the old assertion passed only because len/5==200 for exactly 1000 chars). 28 lifecycle_manager tests green. |
| E10 | Hash-duplicate recency tiebreaker never applied - a real fix. The hash phase (find_duplicates_by_hash) calls _sort_by_priority(group) and takes [0] as primary. That sort key was tier*10 + confidence*5 + (1 if c.created_at else 0); the recency term is a CONSTANT (every chunk has created_at -> always 1), so among equal tier+confidence duplicates the stable sort just kept INPUT ORDER - "oldest wins" was never implemented. The sibling path _determine_primary (embedding/cross-tier phases) DID implement oldest-wins via a conditional, so the two phases could disagree on orientation. Fix (R1/R2/R5): replace the weighted-sum key with a lexicographic tuple (TIER_PRIORITY, confidence, -created_at.timestamp()) sorted reverse=True - higher tier, then higher confidence, then OLDEST first (negated timestamp); a chunk missing created_at sorts last (preserves the old present-beats-missing semantic). Removes the magic *10/*5 weights and makes the hash tiebreak consistent with _determine_primary. _determine_primary itself is unchanged (correct for its inputs; its None-comparison fragility is out of E10 scope). | 5137077; 2 tests (equal tier+confidence, only created_at differs -> oldest is primary for BOTH input orders [older,newer] and [newer,older] - fails old code on [newer,older]: "oldest must win as primary: assert 'newer' == 'older'"; a higher-tier-but-newer chunk still beats a lower-tier-older one -> tiebreak does not override tier). E5 orientation + cluster test docstrings/comments refreshed: post-E10 the hash phase agrees with the embedding phase on oldest=primary, so those tests now pin _pair_key/unique-target dedup with same-orientation pairs (assertions are computed from result.pairs, so they held across the change). 7 dedup tests green. |
| E11 | get_candidates_for_cleanup ValueError on NONE severity - a real fix. The method built a local severity_order = [LOW, MEDIUM, HIGH, CRITICAL] (omitting DriftSeverity.NONE) and then called severity_order.index(min_severity) and severity_order.index(signal.severity). But _score_to_severity returns DriftSeverity.NONE for any drift_score below the LOW threshold, and detect_confidence_drift emits a signal whenever confidence < confidence_threshold using severity=_score_to_severity(drift_score) - so confidence just under the threshold (e.g. 0.38 with threshold 0.4 -> drift_score 0.05) produces a NONE-severity signal that analyze_chunk appends ungated to report.signals. .index(NONE) then raised "ValueError: <DriftSeverity.NONE: 'none'> is not in list" (also if a caller passed min_severity=NONE). Fix (R2/R8): add DriftSeverity.NONE as the lowest rung of severity_order, making .index() total over the enum - NONE signals rank lowest and are included only when min_severity=NONE. | a5d208f; 3 tests (NONE-severity signal in report -> no raise, excluded at default MEDIUM while a real MEDIUM signal is returned; min_severity=NONE -> no raise, includes the NONE rung; end-to-end detect_confidence_drift(0.38) emits severity==NONE and feeding it through get_candidates_for_cleanup does not raise). All 3 fail against the old list: "ValueError: <DriftSeverity.NONE: 'none'> is not in list". |
| H3 | Quality-gate weighted_min discards weighting - a real fix. The weighted_min branch computed weighted_scores = [s.score * w for s, w in zip(scores, weights)], took min_idx = weighted_scores.index(min(...)), then set overall_score = scores[min_idx].score - the RAW (unweighted) score of the min-weighted check. So gate_weights (e.g. create_memory_quality_gate's memory_relevance=1.2, entity_extraction=0.8) were used only to SELECT the bottleneck check, never to shape the output value; the weighting was discarded. Author intent (evident from the discarded weighted_scores) was the weighted minimum. Fix (R9 restore-intended + R8 range guard): overall_score = min(1.0, max(0.0, min(weighted_scores))) - the weighted minimum itself, clamped to [0,1] since a weight > 1 can push a weighted score past 1.0. With uniform weights (all 1.0) this reduces to the plain minimum, so the default path is unchanged. | f034eed; 3 tests (non-uniform weights a(0.8)*0.5=0.40 vs b(0.6)*1.0=0.60 -> overall==0.40 not the raw 0.80 - fails old code "assert 0.8 == 0.4"; uniform weights -> plain min 0.60 preserved; weights >1 with perfect scores -> weighted min 1.2 clamped to 1.0). |
| H4 | rule_deployment remove unguarded - a real fix. _apply_change's deployment "remove" branch did content = content.replace(change.current_value, "") then wrote the file, with NO guards (modify, right above it, guards "current_value not in content"). Two failure modes: (a) no-op + false success - if current_value was absent replace did nothing but the method returned (True, ""); the dry-run path also validated current_value only for modify, not remove (it checked file existence only). (b) file-wide strip - str.replace removes EVERY occurrence, so a current_value appearing more than once is deleted throughout the file. Fix (R8, R2 - make remove consistent with modify): require current_value present in BOTH dry-run and deployment, and (remove only) reject empty current_value; remove a SINGLE occurrence via replace(current_value, "", 1) instead of all. The non-empty guard is scoped to remove - empty modify is a separate pre-existing issue left unchanged so dry-run and real modify still agree. _verify_change records the expected post-remove occurrence count in _apply_change and checks it (a still-present second copy after single removal is NOT a failure; a remove with no recorded apply cannot be proven post-hoc -> not verified). Substring matching is still inherently fragile (a sub-word current_value clips mid-word) - shared with modify, out of H4 scope. | 25592f5; 7 tests (absent value -> (False, err), file untouched - fails old code "assert True is False"; value twice -> exactly one removed, count==1, not file-wide; empty current_value -> False; dry-run absent value -> False not the old existence-only True; unique value -> removed, neighbors kept; verify agrees with single-occurrence remove after apply; verify without a recorded apply -> not verified). |
| H5 | Category-drift fabricated from a 0.5 default - a real fix. _detect_category_drift splits the window into first/second halves, computes per-category success rates, then loops over set(first_rates) | set(second_rates) (the UNION) with first_rates.get(cat, 0.5) / second_rates.get(cat, 0.5). A category present in only ONE half had its missing-half rate defaulted to 0.5, so e.g. a category at 1.0 in the first half but absent in the second produced drift = 0.5 >= 0.15 and a fabricated CATEGORY_DRIFT pattern ("dropped by 50%") -> false rule proposals, from data that did not exist in the other half. Fix (narrow, R8/R2): iterate the INTERSECTION set(first_rates) & set(second_rates) and index directly (no 0.5 default), so only categories with a real baseline in both halves are compared; a category in just one half yields no drift. Detecting that a category appeared/vanished is a different (volume) signal, not success-rate drift - out of scope. | 748e55d; 2 tests (category A only in first half + B only in second half -> zero CATEGORY_DRIFT patterns - fails old code "assert {'A'} == set()"; category A in both halves with a real 1.0 -> 0.0 rate change -> one drift pattern with first_rate 1.0/second_rate 0.0, real detection preserved). |
| H6 | Usage aggregator labels each daily bucket one day early - a real fix. get_daily_breakdown builds, for i in range(days), a rolling 24h window day_end = now - i days and day_start = day_end - 1 day, then labels the bucket "date": day_start.strftime("%Y-%m-%d"). The i=0 window ends at now (today) but was labeled day_start = yesterday, and every bucket was likewise shifted one day early; an op from earlier today landed under yesterday's date, and there was no bucket for today at all. Fix (narrow): label by day_end ("date": day_end.strftime(...)). The windows fed to _filter_operations and therefore the counts are unchanged; timezone handling (datetime.now(timezone.utc), fromisoformat) is unchanged - only the label moves. | 8b5c21b; 2 tests (op 1h ago -> appears under today NOW.date() not yesterday; the most recent bucket is labeled today - fails old code "assert '2024-06-14' == '2024-06-15'"; 3 ops across days -> all still counted once, 7 buckets, labels run day_end-dated from today back). |
| H7 | Quality-trends mixes statistics - a real fix. _analyze_metric split values into first_half and second_half, used their means for previous_value/current_avg, change_percent, and direction, but reported current_value = values[-1] and evaluated warning/critical thresholds against that final sample. That made one trend object mix two different "current" statistics: a good last sample could hide a bad current-period average, and a bad last sample could overstate a healthy current period. Fix (R2/R8): current_value is now the second-half mean used by the trend math, and warning/critical checks use that same value. | 1e5263f; 2 tests (higher-is-better confidence values [0.90, 0.90, 0.70, 0.95] -> previous 0.90, current 0.825, warning True despite good last sample - fails old code with current 0.95 and warning False; lower-is-better escalation values [0.10, 0.10, 0.40, 0.05] -> current 0.225 and warning True despite good last sample - fails old code with current 0.05 and warning False). |
| H8 | build_namespace_key truncation/collision - a real fix. build_namespace_key iterated the expected segment order but `break`ed on the first missing value, despite the comment saying "Use placeholder for missing optional segments." A sparse key with later values (e.g. agents category + project + timestamp but no type) collapsed to only the prefix ("agents:coder"), dropping populated fields and making distinct sparse keys collide. Fix (R2/R8): interior missing segments append an empty placeholder and continue up to the last provided segment, so later positions survive (`agents:coder::memory-mcp:...`) while trailing missing segments keep the old compact prefix shape (`agents:coder`, not `agents:coder:::`). parse_namespace_key now treats empty placeholders as absent values rather than real segment content, preserving later segment names without shifting. | c30f63d; 3 tests (missing middle segment -> key includes placeholder and later fields, fails old code with "agents:coder"; trailing missing segments stay compact - catches over-padding; sparse findings key with missing severity -> parse round-trips agent and id without shifting id into severity, fails old code because id was dropped). |
| H9 | Approval rate can exceed 1.0 - a real fix. submit_for_approval auto-approved low-risk proposals before incrementing total_submitted, but still incremented approved. get_stats then computed approval_rate as approved / max(1, total_submitted - pending), so an auto-approved proposal produced approved=1, total_submitted=0, and later mixed auto/manual flows used a numerator and denominator from different populations. Fix (R2/R8): total_submitted increments once at the start of submit_for_approval, before either auto or manual branches, so every submitted proposal participates in the same accounting base. | 03fedf4; 2 tests (single auto-approval -> total_submitted==1, approved==1, pending==0, approval_rate==1.0 - fails old code with total_submitted 0; one auto-approved + one manual rejected -> total_submitted==2, approved==1, rejected==1, approval_rate==0.5 - fails old code with total_submitted 1). |
| H10 | limit*2 recall cap in outcome retrieval - a real fix. get_outcomes_by_category took the newest `limit * 2` ids from the category index, then applied missing-record and `since` filters. A tail of stale records could therefore consume the pre-scan window and hide older but still in-window matches. The same over-fetch bug existed in get_outcomes_by_type. Fix (R2/R8): both methods delegate to `_get_filtered_outcomes`, which scans the full index newest-first, skips missing records, applies `since`, then stops once `limit` matching outcomes are returned. | 3fabb6c; 2 tests (category query with 2 recent matches followed by 8 stale records, limit=2, since=1 day -> returns the 2 recent matches; type query same shape -> returns the 2 recent matches. Both fail old code with empty results because the newest limit*2 tail was all stale). |
| H11 | Tag scorer shared cached dict / content[:300] key - a real fix. TagAssignmentScorer.assign_tags used `content[:300].lower()` as the entire cache key, even though full content and context both affect WHO/WHEN/PROJECT/WHY assignment, and returned the cached mutable dict directly. Fix (R2/R8): cache key is now a SHA-256 hash of full content + context (json-encoded with stable key order), and cached TagAssignment maps are deep-copied on write and read so caller mutation cannot corrupt cache state. | 2c60c26; 3 tests (caller mutates returned WHO value and PROJECT components -> next cached read returns an independent, unmutated copy; two contents with identical 300-char prefixes but different project keywords -> memory-mcp vs trader-ai, old code returned memory-mcp twice; same content with different agent_id contexts -> codex vs claude, old code returned codex twice). |
| G12 | Startup/tunnel automation unreliable - a real ops fix. The server launcher used Get-Process.CommandLine (not reliably available), could miss an already-running server, and treated failed health verification as a warning. The tunnel launcher reused the same unreliable process detection, could start a public tunnel even when the local server was unhealthy, could report a stale saved Cloudflare URL, and reused one temp output file. The tracked batch launcher started from scripts/ and pointed at scripts/venv-memory. Fix: server startup gates on /health, uses Win32_Process command lines, refuses duplicate/unhealthy process states, launches the repo venv python directly from repo root, and fails if health never comes up. Tunnel startup refuses public exposure unless local /health is healthy, detects existing cloudflared by command line, verifies saved tunnel URLs before reporting, removes stale URL files, uses $cloudflared.Source, and uses a unique temp output file. Batch startup pushd's to repo root and points at the repo venv. | 264dfda; 4 static smoke tests (server script uses Win32_Process + health gate and no longer has the warning-only health path; tunnel script refuses unhealthy local server, verifies/removes stale URL, uses resolved cloudflared and unique output; batch script runs from repo root). |

### Priority pass (prior, commits 0f6c13b..066e732) - verified by code-trace
A1, A2, B1 (ID propagation), C1, C2, D1, D3, D4, D6, E1, E2, E3, G1, G2, G3 (write side),
H1. See MECE doc Section "Highest-priority fix order". NOTE: both prior REGRESSION/INCOMPLETE
items (G3 read side, B1a HippoRAG) are now FIXED and verified - see Section 1 reliability/retrieval.

---

## 2. Still failing - by test file, mapped to MECE ID

0 failures remain (24 -> 17 after G7 -> 12 after G3/G11 -> 5 after retrieval -> 0 after the
final 5). All Section 2 items are FIXED (committed or in tree). The untested backlog and CI
items in Sections 3-4 remain.

### Proactive injection - 7 test failures - FIXED (2026-06-13, commit 0d8cfe7)
| Test | Line | Reason |
|------|------|--------|
| test_proactive_context_injector::test_injection_history | 308 | assert 0 >= 1 (no history recorded) |
| test_proactive_integration::test_file_open_injection_flow | 158 | assert None is not None (no context injected) |
| test_proactive_integration::test_activity_pattern_injection_flow | 198 | assert None is not None |
| test_proactive_integration::test_execution_mode | 480 | assert None is not None |
| test_proactive_integration::test_planning_mode | 493 | assert None is not None |
| test_proactive_integration::test_brainstorm_mode | 506 | assert None is not None |
| test_proactive_integration::test_nexus_processor_failure | 557 | Should not raise on nexus failure |

CORRECTION (audit, 2026-06-13): these 7 failures are NOT MECE G7. Root cause was
(a) shared-mutable-DEFAULT_RULES contamination - test_disable_rule disabled the
file-open rule globally and leaked to 6 tests; (b) relevance ignored trigger
confidence so a 0.9-confidence activity pattern (0.69 chunk relevance) fell under
its 0.7 threshold. Fixed in proactive_context_injector.py: deepcopy rules per
injector + blend trigger confidence into _calculate_relevance. These are real but
separate from the G7 code defect below.

### MECE G7 (activity-detector cooldown on failure) - FIXED + regression test (2026-06-13, commit 0a79c44)
The audited G7 defect was in activity_detector._trigger_pattern: _recently_triggered.add()
ran AFTER `await injector.handle_trigger()` inside the try, so on injector failure the
except swallowed the error and NO cooldown was recorded -> the pattern re-fired every
detection cycle, unbounded. NOT touched by the injector change above; surfaced by external
audit. Fixed by moving cooldown registration into a `finally` (records cooldown/backoff
regardless of success). Regression test added: test_trigger_pattern_records_cooldown_on_injector_failure
(verified to fail on the pre-fix code, pass after).

### Obsidian read-sync - MECE G3 (read side) - FIXED (2026-06-13, commits 857aa64/07282a7) - was 2 failures
| Test | Line | Reason (pre-fix) |
|------|------|--------|
| test_integrations::test_sync_vault_finds_markdown_files | 183 | each file failed with 'list' has no attribute 'tolist' -> files_synced=0 |
| test_integrations::test_sync_vault_returns_chunk_count | 197 | total_chunks=0 |
Two layers. (1) PRODUCT incomplete-fix: vault_sync_service._sync_single_file (and the dead
sibling obsidian_client._sync_file) did naive embeddings.tolist(); the ingestion path already
normalized ndarray-or-list (memory_ingestion_service._encode_single_chunk), the sync path never
did -> breaks on any list-returning embedder (qwen3vl / mocks). Applied `if hasattr(.,'tolist')`.
(2) TEST-STALE: the chunk mock stubbed chunk_file while the code calls chunk_text -> unstubbed
mock returned empty/len-0, so files "synced" with 0 chunks. Fixed the mock to stub chunk_text.

### Beads - MECE G11 - FIXED (2026-06-13, commits 09abcef/07282a7) - was 3 failures + 2 latent issues
| Test | Line | Reason (pre-fix) |
|------|------|--------|
| test_integrations::test_bead_task_dataclass_fields | 67 | BeadTask.__init__() missing 4 required args |
| test_integrations::test_handle_beads_query_tasks_structure | 108 | BeadTask.__init__() missing 4 required args |
| test_integrations::test_beads_tasks_can_reference_memory | 268 | BeadTask.__init__() missing 3 required args |
Triage VERDICT: over-strict constructor, NOT legitimate drift. description/assignee/created_at/
updated_at are Optional[...] but lacked = None defaults; both from_dict and the tests construct
by keyword (zero positional BeadTask uses in src). Fix: reorder required fields first, default the
nullable fields to None. Tests pass unchanged (no dummy padding).

Beads portability (the audit-flagged hardcoded paths) - FIXED, then standardized after review:
a single resolver beads_bridge.resolve_beads_binary() with precedence MEMORY_MCP_BEADS_CLI ->
BEADS_BINARY (back-compat) -> "bd" on PATH, used by all four surfaces (service_wiring.py:196,
http_server.py:443, beads_tools.py, bead_completion_logger.py). Review caught that the main
BeadsBridge was wired via BEADS_BINARY only, so the new var did not reach it - now unified.
Working dir via MEMORY_MCP_BEADS_DIR -> cwd.

Beads timeout (G11 MECE concern) - FIXED in two layers (review caught the second):
(1) BeadsBridge._run_command did `await process.communicate()` with NO timeout -> wrapped in
asyncio.wait_for(timeout=30) + kill/reap, returns [] on timeout.
(2) The audited surface was ALSO request_router._run_coroutine_sync's unbounded thread.join():
even with (1), a wedge in coroutine startup/cleanup could hang the handler. Added an optional
timeout (coro wrapped in wait_for + bounded join with is_alive backstop); the 3 Beads handlers
pass _BEADS_SYNC_TIMEOUT=35s (just above the inner 30s). Regression tests:
test_run_command_times_out_and_kills_process + test_run_coroutine_sync_bounds_wait_with_timeout.

### HippoRAG retrieval - MECE B1a - FIXED (2026-06-13, commit 358ea67) - was 1 failure
| Test | Line | Reason (pre-fix) |
|------|------|--------|
| test_hipporag_integration::test_retrieval_returns_relevant_chunks | 440 | assert 0 > 0 (retrieve returns nothing) |
VERDICT: B1 was FULLY fixed - this was NOT graph-to-chunk mapping. Trace showed get_node('tesla')
True, PPR converged, chunk mapping worked for richer queries. Root cause = entity extraction:
spaCy NER returns 0 entities for a bare keyword query ("Tesla", no sentence context), so retrieve
bailed before PPR. Fix: keyword fallback _match_query_tokens_to_nodes() matches the normalized
query phrase/tokens against graph nodes when NER finds nothing; wired into retrieve() and
_get_query_nodes (multi-hop). The previously-failing test now guards it.

### Nexus tier integration - MECE D (fusion e2e) - FIXED (2026-06-13, commit 8d76d00) - was 1 failure
| test_mcp_tools_e2e::test_nexus_process_with_graph_integration | 340 | Should return results from integrated tiers |
Separate bug, not the graph-mapping issue. Trace: recall returned 5 candidates but the FILTER
dropped all. The hybrid score is a weighted SUM (vector .4 / graph .4 / bayesian .2), so a
vector-only candidate caps at 0.4 -> a 0.576 similarity became 0.23, under the flat 0.3 threshold.
Single-tier matches (the common case) were systematically filtered out. Fix: filter on the
strongest per-tier evidence (max of vector/graph/bayesian); ranking still uses the hybrid.

### Observation timeline - time math (E2 timezone basis) - FIXED (2026-06-13, commit be1f0f7) - was 1 failure
| test_phase3_search_enhancements::test_hours_back_computes_after | 266 | assert 14400.0 < 5 |
PRODUCT bug: handle_observation_timeline computed the `after` cutoff with datetime.now()
(local) but observations are stored with datetime.utcnow(); 14400s = the 4h EDT offset, so the
query window was skewed by the host's UTC offset. Fix: use datetime.utcnow() (matches storage).

### Tagging protocol - TEST-STALE (brittle) - FIXED (2026-06-13, commit 17a9453) - was 1 failure
| test_phase2_integration::test_tagging_includes_all_fields | 129 | WHO (agent) field missing |
The code DOES set the WHO field (_enrich_metadata_with_tagging: enriched["agent"]={...}). The
test grepped source TEXT for the single-quoted literal 'agent' while the code writes "agent"
(double quotes). Fix: assert on the runtime enriched dict (contract is runtime metadata), not
source-text quote style. No product change.

### Ontology bridge - PRODUCT - FIXED (2026-06-13, commit 8c42a62) - was 2 failures
| test_ontology_bridge::test_add_cross_link_life_to_beads | 275 | wrong CrossLinkType (references-task vs owns-task) |
| test_ontology_bridge::test_query_execution_mode | 386 | assert 0 > 0 (empty beads) |
Two distinct PRODUCT bugs. (1) get_cross_links read link_type at the top of edge_data, but
add_relationship nests fields under edge_data["metadata"], so it always fell back to the default
MEMORY_REFERENCES_TASK; fix reads the nested metadata via get_edge_data. (2) _query_beads queried
the external bd CLI (self.beads) instead of the in-graph beads entities the bridge stores; fix
searches list_beads_entities(), consistent with _query_life/_query_projects.

### Tool count - TEST-STALE - FIXED (2026-06-13, commit 17a9453) - was 1 failure
| test_phase4_mcp_tools::test_handle_list_tools_returns_thirteen_tools | 31 | Expected 13 tools, got 14 |
14 tools legitimately exist (added unified_search/bayesian_inference/lifecycle_status/beads*).
Fix: assert the exact expected tool-name SET (catches additions and removals, names the diff)
instead of a brittle magic-number count; renamed to test_handle_list_tools_returns_all_tools.

### Vector search validation contract - TEST-STALE - FIXED (2026-06-13, commit 196c815) - was 5 failures
| Test | Line (src) | Reason (pre-fix) |
|------|-----------|--------|
| test_initialization_missing_embeddings_config | vector_search.py:21 | expected AssertionError, code raises ValueError |
| test_initialization_missing_storage_config | vector_search.py:21 | same |
| test_execute_empty_query_fails | vector_search.py:27 | same |
| test_execute_zero_limit_fails | vector_search.py:31 | same |
| test_execute_limit_too_large_fails | vector_search.py:33 | same |
VERDICT: code is CORRECT, tests were stale. Validation was deliberately moved from assert to
raise ValueError (helper docstrings: "without relying on assert statements", "under normal and
optimized Python") because asserts are stripped under python -O, silently removing the checks.
Fix: the 5 tests now expect ValueError. No product change.

### Summary of Section 2 (0 remaining)
- FIXED 2026-06-13: G7, G3, G11, B1a HippoRAG, D-nexus fusion, vector-search contract, plus the
  final 5 - observation-timeline (utcnow basis), tagging (behavioral test), ontology x2 (nested
  edge metadata + graph-based beads query), tool-count (name-set assertion).
- PRODUCT bugs still open in Section 2: **0**
- TEST-STALE still open in Section 2: **0**
- Total still failing: **0** (suite is green on tests; coverage gate + untested backlog remain - Sections 3-4).

---

## 3. Open in MECE but NOT covered by any failing test

These have no test asserting them, so they are silent until exercised. From the MECE doc,
still open and untested (or test passes but code is suspect per audit):
- **Security:** A3 - now VERIFIED FIXED (5adb4df) + tested (Section 1 Backlog pass); was the
  last open security item.
- **Retrieval:** none open. D7 (Bayesian VAR=state rows as docs) now FIXED (structural _is_bayesian_pseudo_doc filter applied after rank, before rerank) + tested - Section 1. With D2, D5, D8 (all VERIFIED FIXED in fe2f9946 + tested), the WHOLE D-cluster is done.
- **Lifecycle:** none open. WHOLE E-cluster done + tested (Section 1 Backlog pass): E4 (VERIFIED FIXED 5adb4df, pinned), E5 (fake dedup counts), E6 (hour-0-only cleanup, elapsed-time gating), E7 (no cross-scheduler lock, RLock + @guarded_mutation), E8 (str(dict) archive parse, honest file_path parse), E9 (lossy summary bound, SUMMARY_MAX_LEN budget), E10 (recency tiebreaker, oldest-wins lexicographic key), E11 (DriftSeverity.NONE ValueError, NONE added to severity_order).
- **Reliability:** none open. G12 startup/tunnel automation is fixed with static smoke tests; G8, G9, G10, G6, G4, G5 also FIXED + tested - see Section 1.
- **Config:** F2 (live Codex config -> stale worktree; example versioned only), F5 (requirements vs pyproject drift)
- **Validation:** none open. H3 (weighted scoring - REAL FIX, weighted_min now honors gate_weights), H4 (rule removal - REAL FIX, guarded single-occurrence remove), H5 (category-drift - REAL FIX, intersection not union-with-0.5-default), H6 (daily bucket one day early - REAL FIX, label by day_end), H7 (quality-trend statistic consistency - REAL FIX, current_value/thresholds use second-half mean), H8 (namespace placeholders preserve later segments - REAL FIX), H9 (approval-rate accounting - REAL FIX), H10 (outcome recall filters before limit - REAL FIX), and H11 (tag-scorer cache key + copied cached values - REAL FIX) done + tested - Section 1 Backlog pass.
- **V-QWEN - FIXED (commit cd07182):** test_qwen3vl_embedder.py::test_is_available_when_model_not_loaded was
  environment-sensitive. is_available() reads the `model` property, which LAZY-LOADS via _load_model() when
  enabled and _model is None; the bare assertion therefore failed on any machine/run where the model loaded
  (observed once during a D7 full suite, green on re-run). Now hermetic: _load_model is patched to a no-op so the
  property cannot pull in a real model, _model stays None, and is_available() is deterministically False. (Setting
  _model=None alone is insufficient - the property would re-trigger the load.) Focused qwen 29 passed; full suite
  1293/0 deterministic on this machine (where the model DOES load, so it would have failed pre-fix).

Note: G8 (SessionStart cp1252 print crash) is now FIXED + tested (Section 1, Backlog pass).
STILL DEFERRED: the hook-file hardcoded paths (claude_code_hooks.py sys.path.insert, *_handler.py
"command" strings) - same class as F3/F4 but in the hook scaffolding; not the print-crash defect.

---

## 4. CI / config items (NOT product bugs)

- **Coverage gate misconfigured for current test shape.** pyproject sets
  `--cov-fail-under=60`; the suite runs ~broad modules with focused tests, so the gate
  fails (and previously masked the real result because collection aborted at ~11%). The
  suite is behaviorally useful at 1234 passing; the gate is a config decision (lower the
  bar, scope cov to tested modules, or grow tests), not a product defect.
- **~945 ruff issues** (497 F401 unused imports, 164 E402, 98 F541, 85 F811, 40 F841,
  39 E712, 17 E741, 5 E722). Mostly auto-fixable; this is the I4 / dependency-CI cleanup
  pass. The 5 E722 bare-excepts overlap the fails-open reliability cluster (A3/G3/G4).
- **New-dep manifest drift (audit 2026-06-13):** the F6 fix added flask==3.0.3/aiofiles==23.2.1
  to requirements.txt but flask>=3.0.0/aiofiles>=23.0.0 to pyproject.toml. This MATCHES the
  repo's existing pin-in-requirements / float-in-pyproject convention (e.g. chromadb), so it is
  consistent rather than newly divergent - but it is the same class as F5 and should be
  reconciled uniformly in the F5 manifest pass, not piecemeal.

---

## 5. Next sequence (agreed)

0. **DONE 2026-06-13 - SUITE GREEN (0 failures):** G7, G3, G11, retrieval cluster (B1a/nexus/
   vector-search), the final 5 (observation-timeline tz basis, tagging behavioral test,
   ontology nested-metadata + graph-beads-query, tool-count name-set), G8 (SessionStart
   UTF-8 emit + store-lifecycle guard), A3 (ConfigGuard fail-open - verified already fixed in
   5adb4df, now pinned), G9/G10 (request_router handler robustness), G6 (TimeScheduler per-schedule
   guard), G4/G5 (file_watcher: capture loop for cross-thread dispatch + single shared handler),
   D5 (network=None fallback - fe2f9946, pinned), D8 (edge-posterior CPD cell/flatten - fe2f9946,
   pinned), D2 (reranker hybrid clamp - fe2f9946, pinned; turned out already-fixed, not a
   product fix after all), and D7 (Bayesian VAR=state pseudo-docs dropped by a structural predicate
   after rank / before rerank - the one genuinely-open D item, a real fix). WHOLE D-cluster done.
   Also V-QWEN (qwen is_available test made hermetic, cd07182), E4 (rekindle last_accessed_ts -
   verified fixed in 5adb4df, pinned), and E5 (fake dedup counts - REAL fix: run_deduplication no
   longer reports found candidates as merged/freed; potential moved to bytes_reclaimable; directional
   pair filter that double-counted swapped-orientation pairs replaced with orientation-independent _pair_key;
   and per-pair summing that double-counted a shared target across a 3+ duplicate cluster replaced with
   unique-duplicate-id summing). And E6 (hour-0-only cleanup - REAL fix: archival/cleanup sweeps gate on
   elapsed time since last run, not wall-clock hour, so a missed hour-0 no longer skips a day's sweep).
   And E7 (no cross-scheduler lock - REAL fix: one threading.RLock on the singleton manager, acquired by
   every mutating entry point via @guarded_mutation, serializes concurrent to_thread mutators of the shared
   Chroma collection; MemorySweeper is not wired live and is outside the current race).
   And E8 (str(dict) archive parse - REAL fix: rekindle parses production JSON metadata, drops the silent
   /default/path.md fabrication, and fails honestly on a parse miss instead of rehydrating the wrong file).
   And E9 (lossy summary bound - REAL fix: a single documented SUMMARY_MAX_LEN=200 budget keeps within-budget
   docs verbatim and bounds larger ones, replacing the miscalibrated min(200, max(50, len/5-1)) that
   needlessly truncated short docs).
   And E10 (recency tiebreaker - REAL fix: the hash-duplicate _sort_by_priority now uses a lexicographic
   (tier, confidence, oldest-first) key so "oldest wins" is actually applied, consistent with
   _determine_primary, instead of a constant recency term that fell back to input order).
   And E11 (DriftSeverity.NONE ValueError - REAL fix: get_candidates_for_cleanup's severity_order omitted
   NONE, so .index() raised on the NONE-severity signals detect_confidence_drift emits; NONE added as the
   lowest rung). WHOLE lifecycle E-cluster (E4-E11) now done.
1. **DONE: G12 (ops/CI).** Lifecycle E-cluster complete; validation H3-H11 done; reliability G12 done.
   - **Validation:** H3-H11 complete.
   - **Reliability:** G12 complete (startup/tunnel static smoke tests; no live Cloudflare integration in unit suite).
2. Next: CI/config (Section 4): coverage-gate decision, I4 ruff sweep, F5 manifest reconciliation,
   plus the deferred hook-file hardcoded paths (claude_code_hooks.py / *_handler.py).

# GATES - memory-mcp completion 2026-09-03 (v10; every gate unmet until its phase)

Run from the worktree root with UNLAZY_SHELL=bash and MMTS_PY exported (locally
D:/Projects/memory-mcp-triple-system/venv-memory/Scripts/python.exe; CI: python3). Every EXECUTABLE GATE script (the 12 in run_self_tests.py MANIFEST; helpers reachability.py,
_embed_stub.py and run_self_tests.py itself are excluded) follows the canonical shape (probe/self_test/main per plan P0.2), inserts the
worktree root on sys.path, prints one success token only after its probe returns True, and exits
non-zero otherwise. `--self-test` asserts `not probe(planted_bad)`, prints exactly
`SELF_TEST_REJECTED <gate-id>`, exits 0. Scripts do not exist yet; Phase 0 writes them and
run_self_tests.py AST-checks the canonical shape before any gate is approved.

OWNS: src/**, tests/**, scripts/**, docs/**, README.md, pytest.ini, audits/**, .github/workflows/**

- [ ] P0-3: eight stale root oracles deleted and unreferenced (excluding .git, audits, archive, docs/connascence, the gate's own target list)
  CHECK: bash -c '"$MMTS_PY" scripts/gates/stale_oracles.py'
  EXPECT: /STALE_ORACLES_GONE absent=8 refs=0 excluded_scan_artifacts=[1-9]\d*/
  EVIDENCE: pending

- [ ] P0-5: baseline is exclusively created, content-hash intact, acceptance summary re-verifies, and a second write is refused
  CHECK: bash -c '"$MMTS_PY" scripts/gates/baseline.py --verify audits/baseline-2026-09-03.json'
  EXPECT: /BASELINE_OK sha256_match=1 rewrite_refused=1 acceptance="12 PASS \| 0 XFAIL \| 0 FAIL" pytest_passed=[1-9]\d* collected=[1-9]\d* nodeids=[1-9]\d* p95_store_ms=\d+ p95_unified_ms=\d+ cov=\d+\.\d+ tools=[1-9]\d*/
  EVIDENCE: pending

- [ ] P0-6: all 12 manifest gate scripts reject their planted-bad fixture, and their CLI branch, probe result and __main__ wiring all match the canonical shape by AST
  CHECK: bash -c '"$MMTS_PY" scripts/gates/run_self_tests.py'
  EXPECT: SELF_TESTS_OK 12/12 modules_accepted=12 shape_violations=0 manifest_matches_disk=1
  EVIDENCE: pending

- [ ] P1d: acceptance 12 PASS under BOTH Bayesian backends and the TypeError adapter is gone
  CHECK: bash -c '"$MMTS_PY" scripts/gates/bayesian_backends.py'
  EXPECT: BAYESIAN_BACKENDS_OK pgmpy=12 lightweight=12 adapter_removed=1
  EVIDENCE: pending

- [ ] P1a: bayesian_inference reports isError on all three failure branches, not on success
  CHECK: bash -c '"$MMTS_PY" scripts/gates/bayesian_honesty.py'
  EXPECT: BAYESIAN_HONESTY_OK no_network=1 unknown_node=1 none_result=1 positive=1
  EVIDENCE: pending

- [ ] P1b: all 9 beads failure combinations are errors, empty is not, HTTP payload carries beads_error
  CHECK: bash -c '"$MMTS_PY" scripts/gates/beads_errors.py'
  EXPECT: BEADS_ERRORS_OK handlers=3 modes=3 cases=9 positive=1 http_response_beads_error=1 request_local=1
  EVIDENCE: pending

- [ ] P1c: three degradation causes are recorded, five stdio handlers and three tri-tier HTTP routes report them, one tier down is not an error and all down is
  CHECK: bash -c '"$MMTS_PY" scripts/gates/tier_degradation.py'
  EXPECT: TIER_DEGRADATION_OK causes=3 stdio_handlers=5 stdio_all_down_error=5 tri_tier_http_routes=3 http_all_down_503=3 unified_top_level=1 clean=1 no_processor=1 processor_exception=1 threads=4 concurrent_leak=0
  EVIDENCE: pending

- [ ] P2a: every registered stdio tool writes one trace row per invocation via both the canonical dispatch and the facade
  CHECK: bash -c '"$MMTS_PY" scripts/gates/trace_every_tool.py'
  EXPECT: /TRACE_EVERY_TOOL_OK tools=[1-9]\d* rows_expected=[1-9]\d* rows_actual_match=1 error_rows=[1-9]\d* trace_failure_isolated=1/
  EVIDENCE: pending

- [ ] P2b: an expired KV row is gone after one stdio ingest, no second cleanup inside 24h, cleanup happens again after the claim expires, and two concurrent ingests clean exactly once
  CHECK: bash -c '"$MMTS_PY" scripts/gates/cleanup_under_stdio.py'
  EXPECT: CLEANUP_UNDER_STDIO_OK expired_row_removed=1 spy_first=1 spy_second=0 reacquired_after_ttl=1 contenders=2 concurrent_runs=1 contention_safe=1
  EVIDENCE: pending

- [ ] P3: the computed reachability dead set is empty, the one call-unreachable module is named with its reason, KEEP paths are present, both servers import, tool count equals baseline, the derived nodeid set difference is exact, and no test fails
  CHECK: bash -c '"$MMTS_PY" scripts/gates/deletion_closure.py --baseline audits/baseline-2026-09-03.json --sha audits/phase3-pre-deletion.json'
  EXPECT: /DELETION_CLOSURE_OK dead_remaining=0 allowed_dead=0 call_unreachable_named=1 checkpoint_clean=1 artifact_gone=1 deleted=[1-9]\d* tools=[1-9]\d* nodeid_diff_exact=1 test_derivation=1 lint=1 passed=[1-9]\d* failed=0 errors=0/
  EVIDENCE: pending

- [ ] P4: the docs oracle flags one planted violation of each of the four reference forms and does not flag the external-block control, and the real docs pass
  CHECK: bash -c '"$MMTS_PY" scripts/gates/docs_truth.py'
  EXPECT: DOCS_TRUTH_OK forms=4 planted_violations=4 external_control_clean=1 real_ok=1
  EVIDENCE: pending

- [ ] P5: coverage ratchet configured at or below the measured TOTAL after deletion
  CHECK: bash -c '"$MMTS_PY" scripts/gates/coverage_ratchet.py'
  EXPECT: /COVERAGE_RATCHET_OK configured=[1-9]\d* measured=\d+\.\d+ ok=1/
  EVIDENCE: pending

- [ ] P6: Railway image (py3.11, no pgmpy) builds, serves the canary via /tools/search and /tools/unified_retrieve against a host embedding stub, reports lightweight, and returns 401 on a wrong key while healthy
  CHECK: bash -c '"$MMTS_PY" scripts/gates/railway_smoke.py'
  EXPECT: RAILWAY_SMOKE_OK backend=lightweight embed_batch=1 canary_search=1 canary_unified=1 wrong_key=401
  EVIDENCE: pending
